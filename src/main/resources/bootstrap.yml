server:
  port: 8888
  
spring:
  application:
    name: config-server
  config:
    name: config-server
  profiles:
    active:
    - native
  cloud:
    config:
      server:
        native:
          search-locations:
          - file:///D:/repository/config-files